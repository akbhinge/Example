Q1. How did viewing a diff between two versions of a file help you see the bug that was introduced?

A1. I was clearly able to distinguish between the changes between two versions of the file


Q. How could having easy access to the entire history of a file make you a more efficient programmer in the long term?

A.

Q.Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?


1. git clone- used to get a repository from github and insert files into the repository.		

Q.How can you use the commands git log and git diff to view the history of files?
A. git diff can be used to get the difference and changes between two commites by providing the commit id. It shows the lines added using green color and lines removed using red color. git log displays all the commits made to the particular file in the repository with a commit id and a commit message to understand what has been committed.